package com.backend.tcc.dto.group;

public record GroupResponseDTO(
        String id,
        String adm,
        String description,
        String image,
        String image_name,
        String name) {
}
